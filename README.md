# demo-nginx-loadbalance
Simple setup to show a Simple Load Balancer using NGINX.

To run:
```
ansible-playbook playbook.yml
```

To see the load-balancer at work:
either:
```
curl localhost
curl localhost
```

or open 'localhost' in your browser, and press F5.
